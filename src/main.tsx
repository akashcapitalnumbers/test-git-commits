import React from "react"
import ReactDOM from "react-dom/client"
import App from "./App.tsx"
import "./index.css"

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)

/* this is not it 

this is supposed to be purple but it not looking purple to my eye*/

/* this are not that bad if i ty to  */
